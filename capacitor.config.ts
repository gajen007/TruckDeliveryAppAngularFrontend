import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.truckdelivery.artsuit',
  appName: 'TruckDeliveryApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
